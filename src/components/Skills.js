import React from 'react'

import { Container } from 'react-bootstrap'

const Skills = () => {
  return (
    <div id="skills">
      <Container style={{alignItems:'center',height: "100vh",justifyContent: 'center',display:'flex'}}>
        <div>
          <h1>This is an skills section.</h1>
        </div>
      </Container>
    </div>
  )
}

export default Skills